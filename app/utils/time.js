/**
 * Time spans in seconds.
 * You have to multiply by 1000 to get values in milliseconds.
 *
 * @type {{MONTH: number, YEAR: number, HOUR: number, SECOND: number, MINUTE: number, WEEK: number, DAY: number}}
 */
module.exports = {
  SECOND: 1,
  MINUTE: 60,
  HOUR: 3600,
  DAY: 86400,
  WEEK: 604800,
  MONTH: 2592000,
  YEAR: 31536000
};
